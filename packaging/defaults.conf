# /etc/server-suite/defaults.conf
# Preserved across package upgrades.
SUITE_DIR=/usr/lib/server-suite
DATA_DIR=/opt/server-suite
LOG_DIR=/var/log/server-suite
SETUP_UI_PORT=7070
DRY_RUN=0
# NOTIFY_EMAIL=admin@yourdomain.com
